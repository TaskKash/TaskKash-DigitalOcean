require("dotenv/config"); const mysql = require("mysql2/promise"); async function run() { const pool = mysql.createPool(process.env.DATABASE_URL); try { const brandSql = `SELECT CASE WHEN up.deviceModel LIKE "Galaxy%" OR up.deviceModel LIKE "Samsung%" THEN "Samsung" ELSE "Other" END as brand, COUNT(DISTINCT u.id) as cnt FROM users u LEFT JOIN userProfiles up ON u.id = up.userId WHERE 1=1 AND up.deviceModel IS NOT NULL AND up.deviceModel != "" GROUP BY brand HAVING brand != "Other"`; const [rows] = await pool.query(brandSql); console.log("Success:", rows); } catch (e) { console.error("SQL Error:", e.message); } process.exit(0); } run();
